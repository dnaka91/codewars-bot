username = @{ (ASCII_ALPHANUMERIC | PUNCTUATION)+ }
add = { "add" ~ username }
remove = { ("remove" | "rm") ~ username }

stats = { "stats" }
help = { "help" }

command = { SOI ~ (add | remove | stats | help) ~ EOI }

WHITESPACE = _{ " " }
